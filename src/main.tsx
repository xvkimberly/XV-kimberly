import React from'react';import ReactDOM from'react-dom/client';import App from'./App';import Admin from'./Admin';import'./styles.css';import'./admin-styles.css';const admin=location.hash==='#/admin'||new URLSearchParams(location.search).has('admin');ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode>{admin?<Admin/>:<App/>}</React.StrictMode>);
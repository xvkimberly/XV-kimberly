import{useEffect,useMemo,useState}from'react';import{Copy,ExternalLink,LogOut,MessageCircle,Plus,RefreshCw,Trash2,Users}from'lucide-react';import{event}from'./config';
type I={code:string;family_name:string;max_guests:number;attending?:boolean|null;guest_count?:number|null;contact_name?:string|null;message?:string|null};const SITE='https://xvkimberly.github.io/XV-kimberly/';
export default function Admin(){const[secret,setSecret]=useState(sessionStorage.getItem('xv_admin')||''),[input,setInput]=useState(''),[items,setItems]=useState<I[]>([]),[name,setName]=useState(''),[guests,setGuests]=useState(2),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);
const api=async(method='GET',body?:any)=>{const r=await fetch(event.supabase.adminUrl,{method,headers:{'Content-Type':'application/json','x-admin-secret':secret},body:body?JSON.stringify(body):undefined});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403){sessionStorage.removeItem('xv_admin');setSecret('');throw Error('Contraseña incorrecta')}if(!r.ok)throw Error(d.error||'Error del servidor');return d};
const load=async()=>{if(!secret)return;setBusy(true);try{const d=await api();setItems(d.invitations||d.items||[]);setMsg('')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}};useEffect(()=>{load()},[secret]);
const create=async(e:any)=>{e.preventDefault();try{await api('POST',{family_name:name.trim(),max_guests:guests});setName('');setGuests(2);await load()}catch(e:any){setMsg(e.message)}};const url=(c:string)=>SITE+'?invite='+encodeURIComponent(c);const confirmed=useMemo(()=>items.filter(x=>x.attending===true),[items]),declined=useMemo(()=>items.filter(x=>x.attending===false),[items]),people=useMemo(()=>confirmed.reduce((a,x)=>a+(x.guest_count||0),0),[confirmed]);
if(!secret)return <main className="adminLogin"><section className="adminLoginCard"><div className="adminK">K</div><p className="eyebrow gold">ADMINISTRACIÓN</p><h1>XV Kimberly</h1><p>Acceso privado para administrar invitaciones y confirmaciones.</p><form onSubmit={e=>{e.preventDefault();sessionStorage.setItem('xv_admin',input);setSecret(input)}}><label>Contraseña<input type="password" value={input} onChange={e=>setInput(e.target.value)} required autoFocus/></label><button>ENTRAR</button></form></section></main>;
return <main className="adminPage"><header className="adminHeader"><div><small>XV KIMBERLY</small><h1>Administrador</h1></div><button className="iconBtn" onClick={()=>{sessionStorage.removeItem('xv_admin');setSecret('')}}><LogOut size={18}/>Salir</button></header><section className="adminStats"><article><span>Invitaciones</span><b>{items.length}</b></article><article><span>Confirmadas</span><b>{confirmed.length}</b></article><article><span>No asistirán</span><b>{declined.length}</b></article><article><span>Personas confirmadas</span><b>{people}</b></article></section><section className="adminPanel"><p className="eyebrow gold">NUEVA INVITACIÓN</p><h2>Crear liga personalizada</h2><form className="createInvite" onSubmit={create}><label>Familia o invitado<input value={name} onChange={e=>setName(e.target.value)} placeholder="Ej. Familia Martínez" required/></label><label>Lugares<input type="number" min="1" max="20" value={guests} onChange={e=>setGuests(+e.target.value)} required/></label><button><Plus size={18}/>CREAR INVITACIÓN</button></form></section><section className="adminPanel"><div className="panelTitle"><div><p className="eyebrow gold">RSVP</p><h2>Invitados</h2></div><button className="iconBtn" onClick={load}><RefreshCw size={17}/>Actualizar</button></div>{msg&&<p className="status">{msg}</p>}<div className="inviteList">{items.map(i=><article className="inviteRow" key={i.code}><div className="inviteMain"><strong>{i.family_name}</strong><span>{i.max_guests} lugares · <b className={i.attending===true?'ok':i.attending===false?'no':'pending'}>{i.attending===true?'Confirmó '+(i.guest_count||0):i.attending===false?'No asistirá':'Pendiente'}</b></span>{i.contact_name&&<small>Respondió: {i.contact_name}</small>}{i.message&&<small>“{i.message}”</small>}</div><div className="inviteActions"><button onClick={()=>navigator.clipboard.writeText(url(i.code))}><Copy/></button><button onClick={()=>window.open(url(i.code),'_blank')}><ExternalLink/></button><button onClick={()=>window.open('https://wa.me/?text='+encodeURIComponent('✨ Mis XV · Kimberly ✨\n17 de octubre de 2026\n\n'+i.family_name+', acompáñame a celebrar este día tan especial. 💜\n\n'+url(i.code)),'_blank')}><MessageCircle/></button><button onClick={async()=>{if(confirm('¿Desactivar esta invitación?')){await api('DELETE',{code:i.code});load()}}}><Trash2/></button></div></article>)}</div>{!busy&&!items.length&&<div className="empty"><Users/><p>Aún no hay invitaciones.</p></div>}</section></main>}